
on:
  push:
    branches:
      - 'main'
  pull-request:
    branches:
      - 'main'
    types: [opened, reopened]

jobs:
  build-service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ./build.sh